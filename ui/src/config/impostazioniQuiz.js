// Parametri di comportamento del quiz — non testo (per il testo editoriale
// statico vedi config/testi.js). Valori tarabili in un solo punto durante la
// fase di beta, senza toccare la logica dei componenti che li usano.

export const DURATA_AVANZAMENTO_AUTOMATICO_MS = 2500;
