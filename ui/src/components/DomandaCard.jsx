// Una singola domanda con le sue opzioni. Riusata sia in QuizStudente (durante
// lo svolgimento) sia in QuizRisultati (in correzione).
//
// Props previste:
//   - domanda: { testo, opzioni, argomento, ... }
//   - mostraSpiegazione: bool — false durante il quiz (solo ✓/✗), true in correzione
//   - rispostaData / corretta: presenti solo lato correzione
//
// In correzione, mostra sempre il tag dell'argomento della domanda — utile
// per riconoscere le domande "fuori tema" in un quiz misto (es. ripasso).
//
// TODO Fase 1 (versione base) / Fase 4 (tag argomento in correzione).

export default function DomandaCard() {
  return null;
}
