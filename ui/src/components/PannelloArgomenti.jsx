// Vista desktop delle statistiche (layout adattivo, non solo responsive):
// lista argomenti a sinistra, dettaglio (lista quiz + QuizRisultati inline
// quando un quiz è selezionato) a destra. Nessun modale — c'è spazio per
// mostrare lista e dettaglio insieme.
//
// Stessi dati di AccordionArgomenti, stesso QuizRisultati come contenuto:
// cambia solo il contenitore. Usato quando useBreakpoint() indica "desktop"
// (soglia indicativa: ~900-1000px, da verificare a occhio in sviluppo).
//
// TODO Fase 4/5, dopo la versione mobile.

export default function PannelloArgomenti() {
  return null;
}
